interface:
  display_name: "Agent Harness Design"
  short_description: "Design calibrated, evidence-driven agent harnesses"
  default_prompt: "Use $agent-harness-design to review this agent system, distinguish invariants from defaults, and propose the smallest design that fits its real task and risk."
policy:
  allow_implicit_invocation: true
