interface:
  display_name: "Agent Harness Design"
  short_description: "Preserve capability with narrow trusted boundaries"
  default_prompt: "Use $agent-harness-design to review this agent system, preserve authorized model capability, and enforce authority and effects at the narrowest trusted boundary."
policy:
  allow_implicit_invocation: true
